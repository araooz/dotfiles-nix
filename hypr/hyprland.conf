# Script para reiniciar portales y evitar cierres de apps
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user restart pipewire pipewire-pulse wireplumber


monitor= eDP-1,1920x1080@144, auto,1

###################
### MY PROGRAMS ###
###################
$terminal = kitty
$fileManager = thunar

#################
### AUTOSTART ###
#################
exec-once = waybar & hyprpaper# & hyprlock
exec-once = zen
exec-once = zapzap 
exec-shutdown = docker compose down
exec-shutdown = bluetoothctl power off

#portapapeles
# Iniciar el centinela de cliphist al arrancar
exec-once = wl-paste --type text --watch cliphist store 
exec-once = wl-paste --type image --watch cliphist store

# Atajo de teclado para abrir el historial
bind = SUPER SHIFT, C, exec, cliphist list | rofi -dmenu -display-columns 2 -p "busca papaish" -theme /home/falo/.config/rofi/launchers/type-1/style-5.rasi | cliphist decode | wl-copy
exec-once = rm ~/.cache/cliphist/db                     #para que se reinicie el historial cada q prendo

#brillo
exec-once = swayosd-server

# Comando para forzar el cursor al iniciar
exec-once = hyprctl setcursor Bibata-Modern-Ice 24
#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/
# Definir variables de entorno para el cursor
env = XCURSOR_THEME,Bibata-Modern-Ice
env = XCURSOR_SIZE,24






###################
### PERMISSIONS ###
###################
# See https://wiki.hypr.land/Configuring/Permissions/

 ecosystem {
#   enforce_permissions = 1
	no_update_news = true
}

permission = /usr/(bin|local/bin)/grim, screencopy, allow
#permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 4
    gaps_out = 4
    border_size = 2

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(7dcfffee)#rgba(bbbb0088) #rgba(880808ee) rgba(080899ee) 45deg
    col.inactive_border = rgba(7dcfff22)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 5
    rounding_power = 5

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.8

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hypr.land/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = latam
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }

    numlock_by_default = true
}

# https://wiki.hypr.land/Configuring/Variables/#gestures
gestures {
  gesture = 3, up, dispatcher, exec, swayosd-client --playerctl play-pause 
  gesture = 3, down, dispatcher, exec, hyprlock
  gesture = 3, horizontal, workspace
  gesture = 4, up,   dispatcher, exec, swayosd-client --output-volume  100
  gesture = 4, down, dispatcher, exec, swayosd-client --output-volume -100
  gesture = 4, left, dispatcher, exec, swayosd-client --playerctl previous
  gesture = 4, right, dispatcher, exec, swayosd-client --playerctl next


}

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

#desplazarse con WIN + TAB
bind = ALT, TAB, workspace, e+1
bind = ALT SHIFT, TAB, workspace, e-1





# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
#bind = $mainMod, E, exec, thunar
bind = $mainMod, V, togglefloating,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, SPACE, exec, /home/falo/.config/rofi/launchers/type-1/launcher.sh

# abrir, cerrar y reiniciar waybar
bind = $mainMod, XF86Launch2, exec, pkill waybar && waybar&
bind = $mainMod SHIFT, XF86Launch2, exec, waybar&
bind = $mainMod ALT, XF86Launch2, exec, pkill waybar



# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, F11, fullscreen
# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod, S, workspace, 17
bind = $mainMod, D, workspace, 18
bind = $mainMod, E, workspace, 19
bind = $mainMod, W, workspace, 20

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod ALT,   S, movetoworkspace, 17 
bind = $mainMod SHIFT, D, movetoworkspace, 18
bind = $mainMod SHIFT, E, movetoworkspace, 19
bind = $mainMod SHIFT, W, movetoworkspace, 20

# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


## SWAYOSD -----------------
# Brillo de pantalla
bindel = , XF86MonBrightnessUp, exec, swayosd-client --brightness raise
bindel = , XF86MonBrightnessDown, exec, swayosd-client --brightness lower
bindel = , F3, exec, swayosd-client --brightness raise
bindel = , F2, exec, swayosd-client --brightness lower

# Volumen
bindel = , F7, exec, swayosd-client --output-volume raise
bindel = , F6, exec, swayosd-client --output-volume lower
bindel = , XF86AudioMute, exec, swayosd-client --output-volume mute-toggle

# Multimedia
bindl = , F9, exec, swayosd-client --playerctl play-pause
bindl = , F8, exec, swayosd-client --playerctl prev
bindl = , F10, exec, swayosd-client --playerctl next

## --------------------------


#cap de pantalla
bind = $mainMod SHIFT, S, exec, grim -g "$(slurp)" - | wl-copy
#cap de pantalla se guarda en images
bind = ,PRINT, exec, grim -g "$(slurp)" ~/images/capturas/$(date +%Y-%m-%d_%H-%M-%S).png

#admin de tareas
bind = CTRL SHIFT, Escape, exec, kitty -e btop

#bloqueo de pantalla
bind = $mainMod, L, exec, hyprlock
#menu de apagado
bind = $mainMod, F1, exec, /home/falo/.config/rofi/powermenu/type-2/powermenu.sh
#para el wifi
bind = $mainMod, A, exec,  /home/falo/.config/rofi/rofi-wifi-menu.sh

#para el bluetooth
bind = $mainMod, B, exec, blueman-manager








##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
#windowrule = match:class kitty, float on, center on, size 1500 500

# Ignore maximize requests from apps. You'll probably like this.
#windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
#windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

#thunar en ventana

windowrule = workspace 1, match:class zen
windowrule = workspace 20, match:class com.rtosta.zapzap 

windowrule {
  name = thunar
  match:class = thunar

  workspace = 19
  float = on
  center = on
  size = 1000 800
}


#windowrule = match:class thunar, float on

#bluetooth
windowrule = float on, match:class .blueman-manager-wrapped 
#imagenes
windowrule {
  name = imagenes
  match:class = imv

  float = on
  center = on
  size = 1600 900
}

# steam
windowrule = workspace 10, match:class steam 
windowrule = workspace 9, match:class Buckshot Roulette  #hasta q actualicen el windowrule
windowrule = workspace 9, match:class Hollow Knight Silksong  #hasta q actualicen el windowrule


# spotify
windowrule = workspace 17, match:class spotify  
# discord
windowrule = workspace 18, match:class discord

windowrule = workspace 20, match:class ZapZap 




